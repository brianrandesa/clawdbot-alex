# 2026-03-23

## ESA revenue attribution (user)

- **TCV (period context):** $22,500 total contract value.
- **Two wins called out:** $5,000 attributed to **VSL funnel**; $5,000 from an **old client coming back** (reactivation / winback).
- **Implication:** Remaining TCV (~$12,500 in that window) is from other deals/sources; dashboard splits by contact tags (`src-*` and legacy phrase map).

**GHL hygiene:** For clean Lead Source + cash-by-source KPIs, tag contacts with `src-vsl` for VSL path and `src-returning` (or legacy phrases mapped in `api/data.js`) for returning clients.
